[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "workflow-experiment-harness"
version = "0.1.0"
description = "Domain-neutral reproducibility plumbing for Workflow experiments"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
